Documentation https://godoc.org/github.com/kr/jsonfeed

package jsonfeed // import "github.com/kr/jsonfeed"

Package jsonfeed provides types that encode and decode JSON Feed files, as
specified in "JSON Feed Version 1". See https://jsonfeed.org/version/1.

This package's interface might change in the future in a way that requires
updates to client code. If you want to avoid having to update, you should
vendor this package in your application.

func Validity(f *Feed) error
type Attachment struct{ ... }
type Author struct{ ... }
type Feed struct{ ... }
type Hub struct{ ... }
type Item struct{ ... }
